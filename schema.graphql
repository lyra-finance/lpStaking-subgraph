type Stake @entity {
  id: ID!
  txHash: Bytes!
  blockNumber: Int!
  timestamp: Int!
  user: User!
  amount: BigInt!
}

type Withdrawn @entity {
  id: ID!
  txHash: Bytes!
  blockNumber: Int!
  timestamp: Int!
  user: User!
  amount: BigInt!
}

type User @entity {
  id: ID!
  balance: BigInt!
}
